node_modules/
.next/
out/
dist/
build/

# Local data store: Playwright report bundles + extracted assets.
# Anchored to repo root so test fixtures named `data/` are still tracked.
/data/

# Local Playwright outputs from `pnpm --filter @visualize/viewer test`.
# Committed copies live under `screenshots/` once they're approved.
/apps/viewer/playwright-report/
/apps/viewer/test-results/

# Local DATA_DIR extraction dirs (relative ./data resolves per-process cwd
# in dev — see CLAUDE.md). Never commit ingested/seeded run bytes.
/apps/ingest/data/
/apps/viewer/data/

# Local Claude Code agent state.
/.claude/

# env
.env
.env.local
.env.*.local

# logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# os / editor
.DS_Store
.idea/
.vscode/
*.swp

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
prisma/migrations/dev.db*
